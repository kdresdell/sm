#!/bin/bash
FILES=/home/kdresdell/Desktop/*.CSV
for f in $FILES
do
  echo "Processing $f file..."
  # take action on each file. $f store current file name
  /home/kdresdell/Documents/jmdstore/bin/#!/bin/bash
FILES=/path/to/*
for f in $FILES
do
  echo "Processing $f file..."
  # take action on each file. $f store current file name
  cat $f
done
  mv $FILES /home/kdresdell/Documents/
done
